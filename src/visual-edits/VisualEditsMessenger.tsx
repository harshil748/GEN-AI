import React from "react";

/**
 * Shim for Orchids visual edit messenger.
 * Does nothing in local/dev. Prevents module-not-found.
 */
export default function VisualEditsMessenger(): JSX.Element | null {
  return null;
}
